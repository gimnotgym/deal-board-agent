# MEDDPICC LLM 평가 설계 — 한국 IT 영업 맥락 반영 확정본

---

## 1. 입력 소스 및 처리 흐름

### 입력 3경로

```
[A] 짧은 메모       → 딜 상세 화면 텍스트 입력
[B] 문서/통화요약   → 붙여넣기 or 파일 업로드 (딜 상세 화면에서)
[C] 에이전트 대화   → 대화 누적 컨텍스트

※ 모든 입력은 반드시 딜 상세 화면(특정 딜) 진입 후 입력
  → 어느 딜에 매핑할지 모호성 제거
```

### 처리 흐름

```
입력 수신
    ↓
[전처리] MEDDPICC 관련 정보만 추출, 나머지 무시
    ↓
LLM 평가 (기존 점수 + 새 입력 → 재평가)
    ↓
scores + changes + ambiguities 반환
    ↓
[즉시] 점수 변동 항목 있으면 → 미리보기 표시
      (올라간 항목 🔺, 내려간 항목 🔻 + 근거 문장)
    ↓
[즉시] Stage 필수 항목 중 ambiguity 있으면 → 추가 질문
    ↓
영업대표 답변 → 재평가 (Stage 한도까지 반복)
    ↓
"업데이트 하기" 버튼 활성화
```

### 점수 하락 처리 원칙
새 입력이 기존 정보와 충돌할 경우 점수가 내려갈 수 있음.
미리보기에서 🔻 항목과 이유를 명시적으로 표시 후 영업대표가 확인 → 반영.

---

## 2. MEDDPICC 항목별 루브릭 — 한국 IT 영업 맥락

---

### E — Economic Buyer (예산 집행 권한 구조)

**한국화 정의:**
"이 사업의 예산을 집행할 수 있는 권한 구조를 파악했는가"
→ 단일 인물 특정보다 의사결정 구조 파악이 핵심

**한국 현실 유형:**
- 수의계약(소규모): CIO/IT본부장 1명 전결 → 원래 개념 적용
- 경쟁입찰: IT투자심의위원회 통과 필요 → EB가 위원회 전체
- 공공기관: 예산 배정 담당 국장/과장이 실질 EB
- 대형 금융: 분기별 IT예산 집행 원칙 → 타이밍이 EB만큼 중요

| 점수 | 기준 | 예시 표현 |
|------|------|-----------|
| 0 | 파악 안 됨 | 실무 담당자만 언급 |
| 1 | 선 확인 | "팀장/과장 선에서 논의 중", "위에서 검토한다고 함" |
| 2 | 권한자 특정 | "IT본부장이 최종 승인권자 확인", "CIO 직접 면담, 예산 검토 중" |
| 3 | 구조 완전 파악 | "전결 규정 확인 + 분기 집행 일정 파악", "위원회 구성원 및 일정 확인" |

**모호 감지 패턴:** "위에서", "상부에서", "임원 보고 예정", "검토 중"
**추가 질문 예시:**
- "누가 최종 승인권을 갖고 계신지 확인하셨나요? 1인 전결인가요, 위원회 심의인가요?"
- "예산이 이번 분기에 집행 가능한지 확인하셨나요?"

---

### C — Champion (내부 추진력 보유자)

**한국화 정의:**
"고객사 내부에서 이 사업을 우리와 함께 성공시키려는 사람이 있고,
그 사람의 실제 영향력 유형이 확인되었는가"

**한국 Champion 3가지 유형:**

| 유형 | 설명 | 점수 기여 |
|------|------|-----------|
| Type 1 실무 추진자 | 이 사업을 직접 기획·추진하는 사람. 가장 강력하나 결정권 없음 | 2~3점 |
| Type 2 정보 제공자 | 내부 정보를 흘려주는 사람. Champion처럼 보이나 추진력 약함 | 1점 |
| Type 3 정치적 지지자 | 윗선에 우리를 긍정적으로 언급하는 사람. 비가시적이나 영향력 큼 | 2~3점 |

**리스크 신호 (별도 플래그):**
- Champion이 곧 이동/퇴직 예정 → ⚠ Champion 리스크 표시
- Champion이 실무자인데 평가가 외부 위원 중심 → 영향력 제한 주석

| 점수 | 기준 | 예시 표현 |
|------|------|-----------|
| 0 | 없음 | |
| 1 | Type 2 가능성 | "담당 과장이 관심 있어 보임", "자료 요청을 많이 함" |
| 2 | Type 1 확인 | "팀장이 직접 이 사업 기획했다고 함, 정기 소통 중" |
| 3 | Type 1+3 겸비 | "실무 추진자가 임원에게 우리를 적극 추천하고 있다고 직접 언급" |

**모호 감지 패턴:** "담당자가 우호적", "좋아하는 것 같음", "자료를 많이 요청"
**추가 질문 예시:**
- "그 분이 이 사업을 직접 기획하신 분인가요, 아니면 외부 제안을 받아 검토 중인 건가요?"
- "그 분이 윗선에 우리 솔루션을 추천하는 역할을 하고 계신가요?"

---

### M — Metrics (정량적 기대효과)

**한국화 포인트:**
한국 고객은 ROI보다 "비용 절감 금액"이나 "인력 절감"을 더 선호.
공공/금융은 "감사 대응", "규정 준수" 같은 정성 효과도 M으로 처리.

| 점수 | 기준 | 예시 표현 |
|------|------|-----------|
| 0 | 없음 | |
| 1 | 막연한 개선 | "효율화 원함", "비용 줄이고 싶다고 함" |
| 2 | 수치 언급 | "운영비 연 30억 절감 목표", "처리 시간 50% 단축 목표" |
| 3 | 합의 완료 | "KPI 문서화: 업무처리 시간 40% 단축, 인건비 절감 연 15억, 18개월 내 ROI" |

**모호 감지 패턴:** "많이 줄이고 싶다", "빠르게 하고 싶다", "편해지면 좋겠다"
**추가 질문 예시:**
- "현재 이 업무에 몇 명이 투입되고 있는지, 또는 연간 비용이 얼마나 드는지 수치를 들으셨나요?"

---

### DC — Decision Criteria (평가 기준)

**한국화 포인트:**
공식 RFP가 있으면 배점표가 공개됨 → 3점 달성 가능.
수의계약은 배점표 없이 관계/레퍼런스가 기준이 되는 경우 많음.

| 점수 | 기준 | 예시 표현 |
|------|------|-----------|
| 0 | 없음 | |
| 1 | 일반적 기준 | "기술력과 가격을 본다고 함" |
| 2 | 구체 기준 파악 | "RFP 배점: 기술 60%, 가격 30%, 레퍼런스 10% 확인" |
| 3 | 기준 맞춤 대응 완료 | "배점 기준 분석 후 제안서 최적화, 기술 차별점 3가지 부각 완료" |

**모호 감지 패턴:** "좋은 걸 원한다고", "경험 있는 업체를 원한다고"
**추가 질문 예시:**
- "RFP가 나왔나요? 평가 배점 기준을 확인하셨나요?"
- "수의계약이라면 고객이 가장 중시하는 선정 기준이 무엇인지 직접 들으셨나요?"

---

### DP — Decision Process (의사결정 절차)

**한국화 포인트:**
한국 대기업은 결재 단계가 많고 분기말/연말 집행 원칙이 강함.
"언제"만큼 "누구를 거쳐야 하는가"가 중요.

| 점수 | 기준 | 예시 표현 |
|------|------|-----------|
| 0 | 없음 | |
| 1 | 대략적 파악 | "제안서 제출 후 내부 검토한다고 함" |
| 2 | 단계 파악 | "실무→팀장→본부장→임원 결재, 약 3주 소요" |
| 3 | 타임라인 확정 | "결재 단계 및 일정 확정: 6/15 실무검토, 6/30 임원보고, 7/10 계약" |

**모호 감지 패턴:** "검토해보겠다고 함", "내부 논의 중"
**추가 질문 예시:**
- "결재가 몇 단계를 거쳐야 하는지, 각 단계 소요 기간을 파악하셨나요?"
- "연내/분기 내 집행을 원하시는 건지 확인하셨나요?"

---

### PP — Paper Process (계약·발주 행정 절차)

**한국화 포인트:**
한국 IT 사업에서 가장 자주 딜을 지연시키는 요소.
법무검토, 이사회 승인, 조달청 등록, 분기 예산 집행 원칙이 복합 작용.

| 점수 | 기준 | 예시 표현 |
|------|------|-----------|
| 0 | 없음 | |
| 1 | 존재 인지 | "법무 검토가 필요하다고 함" |
| 2 | 절차 파악 | "법무검토 2주 + 이사회 승인 필요, 분기말 집행 원칙 확인" |
| 3 | 준비 완료 | "계약서 초안 법무팀 검토 완료, 서명 일정 조율 중" |

**모호 감지 패턴:** "계약은 나중에", "절차가 있다고 함"
**추가 질문 예시:**
- "계약 전 법무 검토나 이사회 승인 같은 내부 절차가 있는지 확인하셨나요?"
- "예산 집행이 특정 분기에 묶여 있는지 여쭤보셨나요?"

---

### I — Identify Pain (핵심 Pain Point)

**한국화 포인트:**
한국 고객은 Pain을 직접적으로 말하지 않는 경우 많음.
"불편하다", "힘들다" 수준에서 수치화까지 끌어내는 것이 핵심.

| 점수 | 기준 | 예시 표현 |
|------|------|-----------|
| 0 | 없음 | |
| 1 | 일반적 불만 | "현재 시스템이 불편하다고 함" |
| 2 | 구체적 Pain | "레거시로 배치처리 지연, 매일 2시간 수작업 발생" |
| 3 | Pain + 비용 확인 | "수작업 연간 12억 비용 발생, 경쟁사 대비 처리속도 3배 느림 — 직접 수치 확인" |

**모호 감지 패턴:** "불편하다고 함", "개선이 필요하다고 함", "힘들다고 함"
**추가 질문 예시:**
- "어떤 업무에서 가장 불편함을 느끼는지 구체적으로 말씀하셨나요? 예: 수작업 시간, 오류율"
- "그 문제로 발생하는 비용이나 손실을 수치로 말씀하셨나요?"

---

### CO — Competition (경쟁 현황 및 차별화)

**한국화 포인트:**
경쟁입찰에서는 경쟁사 정보가 공개되는 경우 많음 (입찰 참여 업체 공고).
관계 영업이 강한 구조에서는 "현재 SI 업체"가 가장 강력한 경쟁자.

| 점수 | 기준 | 예시 표현 |
|------|------|-----------|
| 0 | 없음 | |
| 1 | 경쟁 존재 인지 | "다른 업체도 볼 것 같다고 함" |
| 2 | 경쟁사 특정 | "A사·B사 입찰 참여, 현재 SI 업체 C사가 메인 경쟁자" |
| 3 | 차별화 확보 | "A사 대비 AI 정확도 15%p 우위 확인, Champion이 우리를 선호한다고 귀띔" |

**모호 감지 패턴:** "경쟁이 있을 것 같다", "다른 데도 볼 것 같다"
**추가 질문 예시:**
- "현재 이 업무를 담당하고 있는 SI 업체가 있나요? 그 업체도 입찰에 참여하나요?"
- "경쟁사 대비 우리 솔루션의 어떤 점이 더 낫다고 고객이 평가하던가요?"

---

## 3. Score 계산 공식

```python
def calc_deal_quality(meddpicc: dict) -> float:
    weights = {"E":2.0, "C":2.0, "M":1.5, "DC":1.5,
               "DP":1.5, "I":1.5, "PP":1.0, "CO":1.0}
    max_score = sum(3 * w for w in weights.values())  # 36.0
    raw = sum(meddpicc[k] * weights[k] for k in weights)
    normalized = (raw / max_score) * 10   # 0~10점
    return round(normalized * 3.33, 2)    # 0~33.3점
```

---

## 4. Stage별 필수 확정 항목 및 추가 질문 한도

| Stage | 최대 추가 질문 | 필수 확정 항목 | 우선순위 이유 |
|---|---|---|---|
| Lead | 0회 | — | 탐색 단계 |
| Identified | 1회 | I | Pain 구체화 집중 |
| Identified-Remind | 1회 | I | 재접촉, 부담 최소화 |
| Identified-Registered | 2회 | I, E | 입찰 참여 결정 시점 |
| Validated | 2회 | I, E, M | 사업 타당성 검증 |
| Qualified | 3회 | M, DC, CO | 경쟁 제안 단계 |
| Negotiated | 3회 | DP, PP, CO | 계약 준비 단계 |
| 우선협상 | 2회 | PP, DP | 계약 임박 |
| 계약완료 | 0회 | — | 종료 |

---

## 5. LLM 평가 프롬프트 (확정)

### 시스템 프롬프트

```
당신은 한국 B2B IT 영업 전문가입니다.
영업 담당자가 입력한 텍스트를 읽고 MEDDPICC 프레임워크로 딜 품질을 평가합니다.

평가 원칙:
1. 명시적으로 언급된 내용만 점수에 반영합니다 (추측 금지)
2. 애매한 경우 낮은 점수를 부여합니다 (보수적 평가)
3. 한국 IT 영업 맥락을 반영합니다:
   - E: 단일 승인자가 아닌 의사결정 구조 파악 여부
   - C: 실무추진자(Type1) / 정보제공자(Type2) / 정치적지지자(Type3) 구분
   - PP: 법무검토, 이사회승인, 분기집행 원칙 등 한국 행정 절차 반영
4. 점수가 올라가거나 내려가면 반드시 근거 문장을 인용합니다
5. 문서/회의록 입력 시 MEDDPICC 관련 정보만 추출하고 나머지는 무시합니다
```

### 유저 프롬프트

```
## 딜 정보
- 사업기회: {사업기회명}
- 고객사: {고객사명} ({산업})
- Stage: {stage}
- 사업유형: {사업유형} (AX/DT/BAU)

## 기존 누적 정보
{기존 주요_진행사항}

## 현재 입력 텍스트
{새 입력 텍스트}

## 현재 MEDDPICC 점수
E:{E} C:{C} M:{M} DC:{DC} DP:{DP} PP:{PP} I:{I} CO:{CO}

## 현재 Stage 필수 확정 항목
{stage_required_items}  ← 예: ["I", "E"]

## 평가 지시
위 루브릭 기준으로 8항목을 재평가하고 아래 JSON으로만 응답하세요.

{
  "scores": {
    "E": 0~3, "C": 0~3, "M": 0~3, "DC": 0~3,
    "DP": 0~3, "PP": 0~3, "I": 0~3, "CO": 0~3
  },
  "changes": [
    {"item": "E", "from": 1, "to": 2, "direction": "up",
     "reason": "IT본부장이 최종 승인권자임을 직접 확인한 내용이 언급됨"}
  ],
  "ambiguities": [
    {
      "item": "E",
      "trigger": "위에서 검토 중이라고 함",
      "is_stage_required": true,
      "question": "누가 최종 승인권을 갖고 계신지 확인하셨나요? 1인 전결인가요, 위원회 심의인가요?",
      "why": "E 항목 — 의사결정 구조 파악 필요 (Validated Stage 필수 항목)"
    }
  ],
  "champion_type": "Type1 실무추진자" | "Type2 정보제공자" | "Type3 정치적지지자" | null,
  "champion_risk": "이동/퇴직 예정 언급" | null,
  "weak_items": ["DC", "PP"],
  "deal_quality_score": 계산값
}
```

---

## 6. 추가 질문 실행 로직

```python
def get_followup_questions(eval_result, stage, asked_count, max_count):
    """
    eval_result: LLM 평가 결과
    asked_count: 이번 대화에서 이미 물어본 횟수
    max_count: Stage별 최대 한도
    """
    if asked_count >= max_count:
        return []  # 한도 소진 → 업데이트 하기 버튼 활성화

    # Stage 필수 항목 중 ambiguity 있는 것 우선
    stage_required = eval_result["ambiguities"]
    priority = [a for a in stage_required if a["is_stage_required"]]
    others   = [a for a in stage_required if not a["is_stage_required"]]

    candidates = priority + others
    remaining  = max_count - asked_count

    return candidates[:remaining]  # 남은 한도만큼만
```

---

## 7. 문서/통화요약 입력 처리 원칙

1. **매핑**: 딜 상세 화면 진입 후 입력 → 해당 딜 자동 매핑 (모호성 없음)
2. **필터링**: 프롬프트에 "MEDDPICC 관련 정보만 추출" 명시
3. **점수 하락 처리**: 기존 정보와 충돌 시 점수 하락 가능
   → 미리보기에서 🔺 상승 / 🔻 하락 항목 명시 + 근거 표시
   → 영업대표 확인 후 반영
4. **긴 문서**: 토큰 초과 방지를 위해 5,000자 이상은 앞부분 요약 처리


---

## 8. 판단 충돌 관리 — 관리자 페이지 (MVP)

### 개념
HITL 반영 결정 시 LLM 평가 점수와 영업대표 최종 결정이 다를 경우
충돌 로그를 저장하고, 관리자가 Few-shot 추가 또는 프롬프트 수정에 활용.

### 충돌 감지 조건
```
LLM 평가 점수 ≠ 영업대표 최종 반영 점수
  예: LLM → E=2, 영업대표 → E=1로 수정 후 저장
  → conflict_log에 자동 기록
```

### 저장 데이터 구조 (conflict_log.json)
```json
{
  "id": "CONF-001",
  "timestamp": "2026-05-28T14:32:00",
  "deal_id": "OPP-2026-006",
  "stage": "Validated",
  "rep_name": "김효진",
  "item": "E",
  "trigger_text": "위에서 긍정적이에요",
  "full_input": "오늘 과장님 통해서 본부장이 우리 솔루션 긍정적으로 봤다고 전달받았어요",
  "llm_score": 2,
  "rep_final_score": 1,
  "llm_reason": "본부장이 긍정적으로 평가했다는 내용이 언급됨",
  "status": "pending"
}
```

### status 값
- `pending` : 미처리
- `added_to_fewshot` : Few-shot에 추가 완료
- `dismissed` : 무시 (예외 케이스로 판단)

### 관리자 페이지 기능 (MVP)
1. 충돌 목록 조회 (pending 우선)
2. 필터: 항목별(E/C/M...) / Stage별 / 담당자별
3. 건별 처리:
   - 정답 점수 선택
   - 판단 근거 메모 입력
   - [Few-shot에 추가] → fewshot_examples.json에 저장
   - [무시] → status = dismissed
4. Few-shot 목록 조회 및 편집
5. 시스템 프롬프트 직접 편집 + 저장

### Few-shot 저장 구조 (fewshot_examples.json)
```json
{
  "id": "FS-001",
  "item": "E",
  "input": "오늘 과장님 통해서 본부장이 우리 솔루션 긍정적으로 봤다고 전달받았어요",
  "correct_score": 1,
  "wrong_score": 2,
  "reason": "전언(과장 → 본부장)은 직접 확인이 아님. E=1이 맞음",
  "created_by": "관리자",
  "created_at": "2026-05-28T15:00:00",
  "source_conflict_id": "CONF-001"
}
```

### LLM 평가 프롬프트에 Few-shot 동적 삽입
```python
def build_prompt(input_text, current_scores, stage):
    # fewshot_examples.json에서 관련 항목 Few-shot 로드
    fewshots = load_fewshots()  # 최대 5개 (최신순)

    fewshot_block = ""
    for fs in fewshots:
        fewshot_block += f"""
예시:
입력: "{fs['input']}"
항목: {fs['item']}
정답: {fs['correct_score']}점 (LLM이 {fs['wrong_score']}점으로 틀린 케이스)
이유: {fs['reason']}
"""
    # 프롬프트에 삽입
    return BASE_PROMPT + fewshot_block + USER_PROMPT_TEMPLATE
```

### API 엔드포인트 (server.py에 추가)
```
GET  /admin/conflicts          충돌 목록 조회
POST /admin/conflicts/{id}     충돌 처리 (정답 선택 + 메모)
GET  /admin/fewshots           Few-shot 목록 조회
POST /admin/fewshots           Few-shot 직접 추가
PUT  /admin/fewshots/{id}      Few-shot 수정
DELETE /admin/fewshots/{id}    Few-shot 삭제
GET  /admin/prompt             현재 시스템 프롬프트 조회
PUT  /admin/prompt             시스템 프롬프트 수정
```

